# راهنمای آپلود روی cPanel و کار با پنل مدیریت

این فایل توضیح می‌دهد وقتی سایت را روی هاست (cPanel) آپلود می‌کنی، تغییراتی که در
پنل مدیریت می‌دهی دقیقاً کجا ذخیره می‌شود و چه تنظیماتی لازم است.

---

## ۱) سایت چطور محتوا را می‌خواند؟

تمام متن‌ها، لینک‌ها، تصاویر گالری و اطلاعات تماس در فایل زیر است:

```
data/content.json
```

مرورگر هر بازدیدکننده این فایل را می‌خواند و صفحه را می‌سازد. پس **هر چیزی که در
این فایل باشد، همان چیزی است که همه می‌بینند.**

پنل مدیریت دو حالت ذخیره دارد:

| حالت | کجا ذخیره می‌شود | چه کسی می‌بیند |
|---|---|---|
| ذخیره محلی (خودکار) | `localStorage` مرورگر خودت | فقط خودت، روی همان مرورگر |
| ذخیره روی سرور (PHP) | فایل `data/content.json` روی هاست | همه‌ی بازدیدکننده‌ها |

> نکته‌ی مهم: پنل هنگام «ذخیره و انتشار» **هر دو کار** را انجام می‌دهد — اول در
> مرورگر ذخیره می‌کند، بعد تلاش می‌کند روی سرور هم بنویسد.

---

## ۲) پس فایل روی cPanel خودکار عوض می‌شود؟

**بله — به شرطی که سه چیز درست باشد:**

1. سایت واقعاً از روی دامنه باز شود (`https://yoursite.com/`)، نه با دوبار کلیک
   روی فایل (`file:///...`). با `file://` هیچ‌وقت PHP اجرا نمی‌شود.
2. فایل `php/save_content.php` روی هاست آپلود شده باشد و هاست PHP داشته باشد
   (تقریباً همه‌ی هاست‌های cPanel دارند).
3. پوشه‌ی `data` روی هاست **اجازه‌ی نوشتن** داشته باشد و توکن درست وارد شده باشد.

اگر این‌ها برقرار باشد، با زدن **«ذخیره و انتشار»** پیام
«ذخیره شد و روی سرور برای همه‌ی بازدیدکننده‌ها منتشر شد» را می‌بینی و فایل
`data/content.json` روی cPanel همان لحظه به‌روز می‌شود.

اگر پیام «سرور PHP در دسترس نیست» آمد، یعنی یکی از شرط‌های بالا برقرار نیست و
تغییرات فقط در مرورگر خودت مانده است.

---

## ۳) تنظیمات لازم روی cPanel (یک‌بار انجام می‌شود)

### الف) دسترسی نوشتن روی پوشه‌ی data
1. وارد cPanel → **File Manager** شو.
2. به `public_html` برو و پوشه‌ی `data` را پیدا کن.
3. روی آن راست‌کلیک → **Change Permissions**.
4. مقدار را روی **755** بگذار. اگر بعداً پیام «پوشه‌ی data قابل نوشتن نیست»
   گرفتی، آن را روی **775** یا در نهایت **777** بگذار.
5. همین کار را برای خود فایل `data/content.json` هم انجام بده (**644** و در صورت
   نیاز **664**).

> در هاست‌های LiteSpeed/CloudLinux معمولاً 755 کافی است، چون PHP با کاربر خودت
> اجرا می‌شود.

### ب) تغییر توکن امنیتی (خیلی مهم)
در فایل `php/save_content.php` این خط را پیدا کن:

```php
$TOKEN = 'mangro-save-v1'; // ← این توکن را تغییر دهید
```

مقدارش را به یک رشته‌ی طولانی و تصادفی عوض کن، مثلاً:

```php
$TOKEN = 'mangro-8Kq2vXpL71bZs4';
```

بعد وارد پنل مدیریت شو → تب **تنظیمات** → فیلد توکن → **دقیقاً همان مقدار** را
وارد کن و ذخیره کن. اگر این دو یکی نباشند، سرور جواب «دسترسی غیرمجاز» می‌دهد.

### ج) تغییر رمز پنل
رمز پیش‌فرض `mangro@1403` است. بعد از اولین ورود، از تب **تنظیمات** حتماً عوضش کن.

---

## ۴) اگر نخواستی PHP فعال کنی (روش دستی)

1. در پنل، تغییرات را بده.
2. دکمه‌ی **«دانلود فایل داده»** را بزن → فایل `content.json` دانلود می‌شود.
3. در cPanel → File Manager → پوشه‌ی `data` → فایل قدیمی را حذف یا rename کن و
   فایل جدید را **Upload** کن.
4. تمام. حالا همه‌ی بازدیدکننده‌ها تغییرات را می‌بینند.

این روش صددرصد کار می‌کند و به هیچ تنظیمی نیاز ندارد، فقط دستی است.

---

## ۵) نکته‌ی مهم درباره‌ی تصاویر

وقتی در پنل یک تصویر را **آپلود** می‌کنی، آن تصویر به‌صورت Base64 داخل خود
`content.json` ذخیره می‌شود. این کار برای پیش‌نمایش خوب است ولی:

- حجم `content.json` را خیلی بزرگ می‌کند
- سرعت لود سایت پایین می‌آید

**روش درست و حرفه‌ای:**
1. عکس را با فرمت `.webp` (یا `.jpg` فشرده) آماده کن.
2. در cPanel → File Manager → پوشه‌ی `img` → **Upload**.
3. در پنل مدیریت به‌جای آپلود، فقط **مسیر** را بنویس، مثلاً:
   ```
   img/slide16.webp
   ```
4. ذخیره و انتشار.

---

## ۶) پیام‌های فرم تماس

فرم تماس به `php/contact.php` ارسال می‌شود و یک کپی هم در مرورگر ذخیره می‌شود
(تب «پیام‌های تماس» در پنل). چون localStorage مخصوص همان مرورگر است، پیام‌هایی
که کاربران می‌فرستند در پنل تو دیده نمی‌شوند. برای دریافت واقعی پیام‌ها:

- در `php/contact.php` ایمیل مقصد خودت را تنظیم کن، یا
- از سرویس ایمیل cPanel (`Email Accounts`) استفاده کن.

---

## ۷) چک‌لیست سریع بعد از آپلود

- [ ] همه‌ی پوشه‌ها (`img`, `js`, `css`, `data`, `php`, `page`, `admin`, `video`, `font`) داخل `public_html` باشند
- [ ] پوشه‌ی `data` روی 755 (یا 775) باشد
- [ ] توکن در `php/save_content.php` عوض شده و در پنل هم همان وارد شده باشد
- [ ] رمز پنل عوض شده باشد
- [ ] سایت با `https://` باز شود (نه `file://`)
- [ ] یک تغییر آزمایشی بده، «ذخیره و انتشار» بزن، بعد از مرورگر دیگر (یا حالت
      ناشناس) سایت را باز کن و ببین تغییر دیده می‌شود یا نه

---

## ۸) عیب‌یابی

| پیام / مشکل | علت | راه‌حل |
|---|---|---|
| «سرور PHP در دسترس نیست» | سایت با `file://` باز شده یا `save_content.php` آپلود نشده | سایت را با دامنه باز کن؛ پوشه‌ی `php` را آپلود کن |
| «دسترسی غیرمجاز» | توکن پنل با توکن فایل PHP یکی نیست | هر دو را دقیقاً یکسان کن |
| «پوشه‌ی data قابل نوشتن نیست» | سطح دسترسی کم است | CHMOD پوشه‌ی `data` را 775 کن |
| تغییرات فقط برای من دیده می‌شود | فقط در localStorage ذخیره شده | «ذخیره و انتشار» بزن یا فایل را دستی آپلود کن |
| تغییرات را نمی‌بینم ولی ذخیره شده | کش مرورگر | `Ctrl + Shift + R` |
